# Incomplete run

This directory contains artifacts from the first unoptimized LPIPS run. The process reached the tool timeout before writing complete metrics/report files. It is preserved only for provenance and must not be used for conclusions.

Use the completed results in `../texture_weight_sweep_v2/`.
